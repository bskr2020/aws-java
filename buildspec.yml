version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies
      - npm install   # or pip install -r requirements.txt
  build:
    commands:
      - echo Build started
      - npm run build
  post_build:
    commands:
      - echo Build completed

artifacts:
  files:
    - '**/*'
